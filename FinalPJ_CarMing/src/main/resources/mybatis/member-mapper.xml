<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="member">

	<select id="login" parameterType="MemberDto" resultType="MemberDto">
		SELECT *
		FROM MEMBER
		WHERE MEMID = #{memid }
	</select>
	
	<select id="selectOne" parameterType="String" resultType="MemberDto">
		SELECT *
		FROM MEMBER
		WHERE MEMID = #{id}
	</select>
	
	<insert id="insert" parameterType="memberDto">
		INSERT INTO MEMBER(MEMNO, MEMNICK, MEMID, MEMPW, MEMNAME, MEMZIP, MEMADDR, MEMADDR2, MEMPHONE, MEMBIRTH, MEMGENDER, MEMCHK) 
		VALUES(MEMSEQ.NEXTVAL, #{memnick}, #{memid}, #{mempw}, #{memname}, #{memzip}, #{memaddr}, #{memaddr2}, #{memphone}, #{membirth}, #{memgender}, #{memchk})
	</insert>
	
	<update id="update" parameterType="memberDto">
		UPDATE MEMBER
		SET MEMNICK = #{memnick}, MEMZIP = #{memzip}, MEMADDR = #{memaddr}, MEMADDR2 = #{memaddr2},
		 MEMPHONE = #{memphone}, MEMBIRTH = #{membirth}, MEMGENDER = #{memgender}, MEMCHK = #{memchk}, MEMFILE = #{memfile}
		WHERE MEMID = #{memid}
	</update>
	
	<delete id="deleteUser" parameterType="String">
		DELETE FROM MEMBER
		WHERE MEMID = #{memid}
	</delete>

</mapper>